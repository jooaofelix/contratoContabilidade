// Chave da API do Portal da Transparência (usada só na consulta de CEIS/CNEP
// da ferramenta "Consulta CNPJ"). É gratuita: peça em
// https://api.portaldatransparencia.gov.br/swagger-ui.html (link "Solicitar
// token de acesso") — chega por e-mail. Cole a chave abaixo entre as aspas.
//
// Sem a chave, a consulta de CNPJ/Simples/MEI continua funcionando normal —
// só a checagem de sanções (CEIS/CNEP) fica desligada.

const TRANSPARENCIA_API_KEY = "";
